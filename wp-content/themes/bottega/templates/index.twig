<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{{ site.name }}</title>
</head>
<body>
    <h1>¡Mi sitio Timber funciona!</h1>

    {% for post in posts %}
        <article>
            <h2><a href="{{ post.link }}">{{ post.title }}</a></h2>
            <p>{{ post.excerpt }}</p>
        </article>
    {% else %}
        <p>No hay posts disponibles.</p>
    {% endfor %}
</body>
</html>
